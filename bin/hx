#!/usr/bin/env node

'use strict';

var fs = require('fs');
var path = require('path');
var exec = require('child_process').exec;
var os = require('os');


var value = process.argv.slice(2);

if(value == "watch") {
    require('../argv/watch.js');
}
// var options = {};


// var help = function () {
// };


// if (args[0] && /^[^-]|\//.test(args[0])) {
//     base = args.shift();
// }


// while (args.length > 0) {
//     value = args.shift();
//     switch (value) {

//         // 监控修改
//         case 'watch':
// 			break;

//         default:

//             if (!base) {
//                 base = value;
//             }
//     }
// }


// if (!base) {
//     base = './';
// }


// if (!fs.existsSync(base)) {
//     process.stdout.write('Error: directory does not exist\n');
//     process.exit(1);
// }
